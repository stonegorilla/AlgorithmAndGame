spring :
  datasource :
    driver-class-name : com.mysql.cj.jdbc.Driver
    url : jdbc:mysql://localhost:3306/algorithmgame?useUnicode=true&characterEncoding=utf8
    username : root
    password : rootpass
    sql-script-encoding : UTF-8
  jpa :
    hibernate :
      ddl-auto : create-drop
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
      encoding: UTF-8